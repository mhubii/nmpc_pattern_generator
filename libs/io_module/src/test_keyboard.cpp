#include <yarp/os/all.h>
#include <yarp/dev/PolyDriver.h>
#include <yarp/dev/SerialDevice.h>

int main() {
    yarp::dev::ISerialDevice *dev;
    // yarp::os::Network yarp;

    // yarp::os::Property config;
    // config.put("device", "ISerialDevice");

    // yarp::dev::PolyDriver dd(config);
    // if (!dd.isValid()) {
    //     std::printf("Not valid.");
    //     return 1;
    // }

    return 0;
}